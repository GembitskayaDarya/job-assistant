# Required — docker-compose refuses to start without it, no default is provided on purpose.
POSTGRES_PASSWORD=
# Optional overrides, default to postgres/job_assistant if unset.
POSTGRES_USER=
POSTGRES_DB=

TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
