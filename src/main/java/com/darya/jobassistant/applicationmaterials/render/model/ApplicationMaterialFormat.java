package com.darya.jobassistant.applicationmaterials.render.model;

/**
 * Sprint 10 Step 4: the physical document format of a rendered artifact - see {@link
 * ApplicationMaterialType} for why this is a separate enum. Only {@link #PDF} exists this step;
 * DOCX is explicitly out of scope.
 */
public enum ApplicationMaterialFormat {
    PDF
}
